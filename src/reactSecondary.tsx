import * as React from 'react';
import * as ReactDOM from 'react-dom';

import SecondScreen from './SecondScreen';

function render() {
  ReactDOM.render(<SecondScreen />, document.body);
}

render();